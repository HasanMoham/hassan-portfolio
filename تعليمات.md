# تعليمات تخصيص وتشغيل البورتفوليو

---

## ٠. إعداد المشروع وتشغيله محلياً

قبل أي شيء آخر، تحتاج إلى تثبيت الحزم وتشغيل المشروع على جهازك.

### المتطلبات الأساسية

- **Node.js** (الإصدار 18 أو أحدث) — حمّله من [https://nodejs.org](https://nodejs.org)
  - للتحقق من التثبيت، افتح الطرفية (Terminal) ونفّذ: `node -v`

### خطوات الإعداد

1. افتح الطرفية (Terminal) داخل مجلد المشروع (المجلد الذي يحتوي على ملف `package.json`).
2. نفّذ الأمر التالي لتثبيت جميع الحزم المطلوبة:
   ```bash
   npm install
   ```
3. بعد اكتمال التثبيت، شغّل بيئة التطوير:
   ```bash
   npm run dev
   ```
4. افتح المتصفح وادخل إلى: [http://localhost:5173](http://localhost:5173)

> **ملاحظة:** مجلد `node_modules` لا يُرسل مع المشروع لأنه كبير الحجم. الأمر `npm install` يُعيد تنزيله تلقائياً من الإنترنت.

---

## ١. استبدال الصورة الشخصية

1. اذهب إلى مجلد `public/assets/` داخل المشروع.
2. احذف الملف الموجود باسم `profile.jpeg`.
3. ضع صورتك الشخصية في نفس المجلد **بنفس الاسم بالضبط**: `profile.jpeg`.
4. يُنصح باستخدام صورة مربعة (مثلاً 500×500 بكسل أو أكبر) لظهور أفضل داخل الإطار الدائري.

---

## ٢. استبدال ملف السيرة الذاتية

1. اذهب إلى مجلد `public/assets/`.
2. احذف الملف الموجود باسم `cv.pdf`.
3. ضع ملف سيرتك الذاتية في نفس المجلد **بنفس الاسم بالضبط**: `cv.pdf`.
4. سيُتاح للزوار تحميله مباشرة عند الضغط على زر "Download CV" في شريط التنقل.

---

## ٣. إعداد نموذج التواصل (Web3Forms)

نموذج التواصل يستخدم خدمة **Web3Forms** المجانية. اتبع الخطوات التالية:

1. اذهب إلى الموقع: [https://web3forms.com](https://web3forms.com)
2. أدخل بريدك الإلكتروني واضغط على "Create your Access Key".
3. ستصلك رسالة تأكيد على بريدك، افتحها وأكّد إنشاء المفتاح.
4. انسخ **مفتاح الوصول (Access Key)** الذي يظهر لك.
5. افتح الملف: `src/components/Contact.jsx`
6. ابحث عن السطر التالي في أول الملف:
   ```js
   const ACCESS_KEY = 'YOUR_WEB3FORMS_ACCESS_KEY'
   ```
7. استبدل `YOUR_WEB3FORMS_ACCESS_KEY` بمفتاحك الفعلي، مثلاً:
   ```js
   const ACCESS_KEY = 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx'
   ```
8. احفظ الملف وأعد نشر الموقع.

---

## ٤. تعديل مشاريع البورتفوليو

1. افتح الملف: `src/components/Projects.jsx`
2. ستجد في أعلى الملف مصفوفة باسم `projects` مع تعليق واضح يشرحها:
   ```js
   const projects = [
     {
       id: 1,
       title: "Project Title",
       description: "وصف المشروع",
       category: "Frontend",
       image: null,
       liveUrl: "#"
     },
     // ...
   ]
   ```
3. عدّل بيانات كل مشروع حسب احتياجك:
   - **title**: اسم المشروع.
   - **description**: وصف مختصر لما يفعله المشروع.
   - **category**: الفئة — اختر إحدى الثلاث: `"Frontend"` أو `"Backend"` أو `"Deployment"`.
   - **image**: رابط صورة المشروع (URL) أو مسار ملف محلي من `public/assets/`، مثل `"/assets/project1.png"`. اتركها `null` إذا لم تكن لديك صورة.
   - **liveUrl**: رابط الموقع الحي للمشروع. اتركها `"#"` إذا لم يكن متاحاً بعد.
4. لإضافة مشروع جديد، انسخ أحد الكائنات الموجودة وعدّل بياناته، مع تغيير `id` إلى رقم جديد.
5. لحذف مشروع، احذف الكائن الكامل من المصفوفة.

---

## ٥. تعديل بيانات التواصل

1. افتح الملف: `src/components/Contact.jsx`
2. ابحث عن المصفوفة `contactInfo` في أعلى الملف.
3. عدّل القيم التالية:
   - **البريد الإلكتروني**: غيّر `hassan.business2325@gmail.com` إلى بريدك.
   - **رقم الهاتف**: غيّر `+201023927016` إلى رقمك.
   - **GitHub**: غيّر الرابط `https://github.com/HasanMoham` إلى رابط حسابك.
   - **LinkedIn**: غيّر الرابط `https://www.linkedin.com/in/hassan2325` إلى رابط ملفك.
4. احفظ الملف.

---

## ٦. النشر على GitHub Pages

### أولاً: إعداد الحساب والمستودع

1. إذا لم يكن لديك حساب GitHub، أنشئ واحداً على [https://github.com](https://github.com).
2. أنشئ مستودعاً جديداً (New Repository):
   - إذا أردت الرابط النظيف `hasanmoham.github.io`، سمّ المستودع بالضبط: **`HasanMoham.github.io`**
   - إذا اخترت اسماً آخر (مثلاً `portfolio`)، افتح ملف `vite.config.js` وغيّر السطر:
     ```js
     base: '/',
     ```
     إلى:
     ```js
     base: '/portfolio/',
     ```
     (استبدل `portfolio` باسم مستودعك بالضبط)

### ثانياً: رفع الكود

افتح الطرفية (Terminal) داخل مجلد المشروع، ثم نفّذ الأوامر التالية بالترتيب:

```bash
git init
git add .
git commit -m "initial commit"
git remote add origin رابط_المستودع_هنا
git push -u origin main
```

استبدل `رابط_المستودع_هنا` برابط مستودعك من GitHub (يظهر بعد إنشاء المستودع).

### ثالثاً: النشر

```bash
npm run deploy
```

هذا الأمر سيبني المشروع تلقائياً وينشره على فرع `gh-pages`.

### رابعاً: تفعيل GitHub Pages

1. اذهب إلى إعدادات المستودع (Settings).
2. اضغط على "Pages" في القائمة الجانبية.
3. في قسم "Source"، اختر الفرع: **`gh-pages`** والمجلد: **`/ (root)`**.
4. اضغط "Save".
5. انتظر من دقيقتين إلى ثلاث دقائق، ثم زُر رابطك.

---

## ٧. الأيقونة (Favicon)

- الأيقونة الحالية موجودة في: `public/assets/favicon.png`
- استبدلها بأيقونتك الخاصة مع الحفاظ على **نفس الاسم بالضبط**: `favicon.png`
- الحجم المُوصى به: **512×512 بكسل**، بصيغة PNG مع خلفية شفافة.
- ستظهر الأيقونة الجديدة في تبويب المتصفح تلقائياً بعد إعادة تحميل الصفحة.
